global:
  resolve_timeout: 5m
  smtp_smarthost: 'smtp.example.com:587'
  smtp_from: 'alertmanager@clearspring.com'
  smtp_auth_username: 'alertmanager@clearspring.com'
  smtp_auth_password: 'YOUR_SMTP_PASSWORD'

# 模板文件
templates:
  - '/etc/alertmanager/templates/*.tmpl'

# 路由配置
route:
  group_by: ['alertname', 'severity']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'feishu'
  
  # 子路由 - 根据严重程度分发
  routes:
    - match:
        severity: critical
      receiver: 'feishu-critical'
      continue: true
    
    - match:
        severity: warning
      receiver: 'feishu-warning'
    
    # 邮件通知（非工作时间）
    - match:
        send_email: 'true'
      receiver: 'email'

# 接收器配置
receivers:
  # 飞书机器人 - 一般告警
  - name: 'feishu'
    webhook_configs:
      - url: 'https://open.feishu.cn/open-apis/bot/v2/hook/YOUR_WEBHOOK_KEY'
        send_resolved: true
        max_alerts: 5
  
  # 飞书机器人 - 严重告警
  - name: 'feishu-critical'
    webhook_configs:
      - url: 'https://open.feishu.cn/open-apis/bot/v2/hook/YOUR_CRITICAL_WEBHOOK_KEY'
        send_resolved: true
        max_alerts: 10
  
  # 飞书机器人 - 警告
  - name: 'feishu-warning'
    webhook_configs:
      - url: 'https://open.feishu.cn/open-apis/bot/v2/hook/YOUR_WARNING_WEBHOOK_KEY'
        send_resolved: true
  
  # 邮件通知
  - name: 'email'
    email_configs:
      - to: 'ops-team@clearspring.com'
        send_resolved: true
        html: '{{ template "email.html" . }}'
  
  # PagerDuty（可选）
  - name: 'pagerduty'
    pagerduty_configs:
      - service_key: 'YOUR_PAGERDUTY_SERVICE_KEY'
        send_resolved: true

# 抑制规则（避免告警风暴）
inhibit_rules:
  # 如果存在 critical 告警，抑制同 alertname 的 warning 告警
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['alertname', 'instance']
  
  # 如果服务宕机，抑制其他相关告警
  - source_match:
      alertname: 'ServiceDown'
    target_match_re:
      alertname: '.*'
    equal: ['instance']
