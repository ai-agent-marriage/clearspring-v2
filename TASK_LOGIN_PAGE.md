# 登录页面开发任务

**任务 ID**: TASK-LOGIN-001  
**优先级**: P0  
**创建时间**: 2026-03-31 14:06  
**截止时间**: 2026-03-31 15:00（1 小时内完成）

---

## 📋 任务描述

创建祈福者端登录页面，支持微信授权登录。

---

## 🎨 设计要求（Stitch 风格）

### 配色方案
- 背景：宣纸底渐变 `#EFEEE9 → #E8E6DF`
- 主色：禅意金 `#D4B87B`
- 辅助色：岱绿 `#4A5D4E`
- 文字：深灰 `#333333`

### 设计元素
- 顶部：欢迎语 + 品牌 Logo
- 中部：登录说明卡片
- 底部：微信登录大按钮
- 液态玻璃效果
- 卡片阴影

---

## 📁 需要创建的文件

1. `/pages/login/login.wxml` - 页面结构
2. `/pages/login/login.wxss` - 样式文件
3. `/pages/login/login.js` - 页面逻辑
4. `/pages/login/login.json` - 页面配置

---

## ⚙️ 功能要求

### 1. 微信登录按钮
```javascript
// 调用微信登录 API
wx.getUserProfile({
  desc: '用于完善用户资料',
  success: (res) => {
    const userInfo = res.userInfo;
    // 保存用户信息
    // 调用云函数登录
    // 返回上一页
  }
})
```

### 2. 云函数调用
调用 `login` 云函数，传入用户信息，获取用户 ID。

### 3. 登录成功处理
- 保存用户信息到本地存储
- 返回上一页（首页）
- 刷新首页用户状态

---

## 📝 注册页面

在 `app.json` 的 `pages` 数组中添加：
```json
"pages/login/login"
```

---

## ✅ 验收标准

1. ✅ 页面正常显示，无编译错误
2. ✅ 微信登录按钮可点击
3. ✅ 能获取用户信息
4. ✅ 能调用云函数成功
5. ✅ 登录成功后能返回首页
6. ✅ UI 符合 Stitch 风格

---

## 🚀 开始执行

**执行步骤**：
1. 创建 4 个文件（wxml/wxss/js/json）
2. 在 app.json 中注册页面
3. 本地测试
4. 提交到 Git

**完成后请通知**：登录页面开发完成，可以测试

---

**任务分配给**: @login-agent  
**监督 Agent**: @quality-agent
